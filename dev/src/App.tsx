import Button from "rollup-build/Button"
import Input from "rollup-build/Input";
import Logo from "rollup-build/Logo"
const App = () => {
  return <div>
    i am app
    <Button contents='' />
    <Input />
    <Logo />
  </div>
}

export default App;